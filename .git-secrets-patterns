# Git Secrets Patterns
# Patterns to prevent committing sensitive data

# AWS Access Keys
(A3T[A-Z0-9]|AKIA|AGPA|AIDA|AROA|AIPA|ANPA|ANVA|ASIA)[A-Z0-9]{16}

# AWS Secret Keys (base64 pattern)
[A-Za-z0-9/+=]{40}

# Generic passwords/secrets
password\s*=\s*['"][^'"]{8,}['"]
secret\s*=\s*['"][^'"]{8,}['"]
api_key\s*=\s*['"][^'"]{8,}['"]

# Email addresses (specific to avoid false positives)
[A-Z0-9._%+-]+@3jdigital\.solutions

# JWT tokens
eyJ[A-Za-z0-9-_=]+\.[A-Za-z0-9-_=]+\.?[A-Za-z0-9-_.+/=]*

# Private keys
-----BEGIN (RSA|DSA|EC|OPENSSH|PGP) PRIVATE KEY-----

# Database connection strings with credentials
(postgres|mysql|mongodb):\/\/[^:]+:[^@]+@[^\/]+
