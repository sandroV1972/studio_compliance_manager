# ============================================
# ENVIRONMENT VARIABLES - PRODUCTION
# ============================================
# IMPORTANTE: Copia questo file in .env e compila con valori reali
# NON committare il file .env su Git!
# ============================================

# ==========================================
# DATABASE
# ==========================================
DB_USER=compliance_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_NAME=studio_compliance
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}?schema=public

# ==========================================
# NEXTAUTH (Autenticazione)
# ==========================================
# URL pubblico dell'applicazione (senza trailing slash)
NEXTAUTH_URL=https://compliance.tuodominio.it

# Secret per JWT (genera con: openssl rand -base64 32)
NEXTAUTH_SECRET=GENERA_UN_SECRET_FORTE_QUI_32_CARATTERI_MINIMO

# ==========================================
# EMAIL - Opzione 1: AWS SES (consigliato)
# ==========================================
EMAIL_HOST=email-smtp.eu-west-1.amazonaws.com
EMAIL_PORT=587
EMAIL_USER=YOUR_AWS_SES_SMTP_USERNAME
EMAIL_PASSWORD=YOUR_AWS_SES_SMTP_PASSWORD
EMAIL_FROM=noreply@tuodominio.it

# ==========================================
# EMAIL - Opzione 2: SMTP Generico
# ==========================================
# Decommenta se usi SMTP invece di SES
# EMAIL_PROVIDER=smtp
# EMAIL_FROM=noreply@tuodominio.it
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ==========================================
# REDIS (Cache e sessioni)
# ==========================================
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# ==========================================
# SUPER ADMIN (primo accesso)
# ==========================================
# Questi valori vengono usati per creare il primo super admin
# all'avvio dell'applicazione (se non esiste già)
SUPER_ADMIN_EMAIL=admin@tuodominio.it
SUPER_ADMIN_PASSWORD=ChangeMe123!StrongPassword
SUPER_ADMIN_NAME=Amministratore Sistema

# ==========================================
# APPLICATION
# ==========================================
NODE_ENV=production
APP_NAME=Studio Compliance Manager
APP_URL=https://compliance.tuodominio.it
APP_PORT=3000

# ==========================================
# STORAGE - Opzione 1: Locale (default)
# ==========================================
STORAGE_TYPE=local
UPLOAD_DIR=/app/uploads

# ==========================================
# STORAGE - Opzione 2: AWS S3
# ==========================================
# Decommenta se usi S3
# STORAGE_TYPE=s3
# S3_BUCKET=compliance-documents-prod
# S3_REGION=eu-south-1
# S3_ACCESS_KEY=YOUR_S3_ACCESS_KEY
# S3_SECRET_KEY=YOUR_S3_SECRET_KEY

# ==========================================
# LOGGING
# ==========================================
LOG_LEVEL=info
LOG_DIR=/app/logs

# ==========================================
# SECURITY
# ==========================================
# Rate limiting (richieste per finestra temporale)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000  # 15 minuti

# ==========================================
# BACKUP
# ==========================================
# Schedule cron per backup (default: ogni notte alle 2 AM)
BACKUP_SCHEDULE=0 2 * * *
# Giorni di retention backup
BACKUP_RETENTION_DAYS=30

# ==========================================
# MONITORING (opzionale)
# ==========================================
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_ENVIRONMENT=production

# ==========================================
# ANALYTICS (opzionale)
# ==========================================
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
